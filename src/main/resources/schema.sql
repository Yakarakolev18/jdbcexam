CREATE TABLE Player (
                        ID INT NOT NULL,
                        Name VARCHAR(255) NOT NULL,
                        Nationality VARCHAR(255) NOT NULL,
                        Birth_date TIMESTAMP,
                        Titles INT,
                        PRIMARY KEY(ID)
);